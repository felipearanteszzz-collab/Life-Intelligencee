{
  "name": "life-intelligence",
  "version": "1.0.0",
  "description": "Life Intelligence — Ferramenta de alta performance pessoal",
  "private": true,
  "scripts": {
    "dev": "npx serve public -p 3000",
    "build": "echo 'Static project — no build step needed'",
    "preview": "npx serve public -p 4000"
  },
  "devDependencies": {
    "serve": "^14.2.3"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
